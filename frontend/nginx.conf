server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback: serve index.html for non-file routes
  location / {
    try_files $uri /index.html;
  }
}


